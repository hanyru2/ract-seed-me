export default function billUpdater(state = 'all', action) {
  /* const { type } = action

  switch (type) {
    case 'SET_FILTER':
      return action.filter

    default:
      return state
  } */

  return state
}
